# Smart_Home_Management
Smart Home Management using Device Control Tree is a C-based project that uses a Binary Search Tree to manage smart devices. It supports CRUD operations through a menu-driven interface. Each device is stored as a node, enabling efficient organization, searching, updating, and deletion using dynamic memory.
